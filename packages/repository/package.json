{
  "name": "@deoxys/repository",
  "version": "0.0.1",
  "description": "A persistence solution for node based on Repository pattern",
  "main": "lib/index.js",
  "types": "./lib/index.d.ts",
  "author": "lorefnon",
  "license": "MIT",
  "private": false,
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "tsc": "tsc -p .",
    "prepare": "tsc",
    "typedoc": "typedoc --out docs",
    "clean": "rm -rf lib docs",
    "burnthemall": "rm -rf lib docs node_modules"
  },
  "devDependencies": {
    "typedoc": "^0.11.1",
    "typescript": "^2.5.2"
  },
  "ava": {
    "files": [
      "lib/**/*.spec.js"
    ]
  }
}
